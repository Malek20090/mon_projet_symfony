{% extends 'admin/layout.html.twig' %}

{% block title %}New expense | Decide${% endblock %}

{% block content %}
<div class="admin-page">
    <div class="page-header-card">
        <div>
            <h1>New expense</h1>
            <p class="mb-0">Add an expense</p>
        </div>
    </div>

    <div class="card-custom">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h5 class="mb-4"><i class="fas fa-credit-card text-primary me-2"></i>Add an expense</h5>
                {{ include('expense/_form.html.twig', { button_label: 'Create' }) }}
                <div class="admin-page-footer mt-4">
                    <a href="{{ path('app_expense_index') }}" class="btn btn-secondary"><i class="fas fa-arrow-left me-2"></i>Back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}


