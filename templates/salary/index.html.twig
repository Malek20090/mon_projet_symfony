<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Salary Dashboard | Decide$</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700;800&family=Playfair+Display:wght@600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
    <link rel="stylesheet" href="{{ asset('css/dark-mode.css') }}">    <style>
        .hero-caption {
            max-width: 900px;
            margin: 0 auto;
            padding: 0;
            text-align: center;
            background: transparent;
        }

        .hero-tagline {
            display: inline-block;
            letter-spacing: 0.14em;
            font-size: 0.78rem;
            color: rgba(255, 255, 255, 0.78);
            margin-bottom: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .salary-hero-title {
            font-size: clamp(2rem, 4.4vw, 4.8rem);
            font-family: "Open Sans", sans-serif;
            font-weight: 300;
            line-height: 1.14;
            color: #fff;
            margin-bottom: 0.75rem;
            letter-spacing: -0.01em;
            text-shadow: 0 4px 14px rgba(0, 0, 0, 0.35);
        }

        .salary-hero-title .accent {
            color: #ffffff;
            font-weight: 600;
        }

        .salary-hero-text {
            color: rgba(255, 255, 255, 0.9);
            max-width: 760px;
            margin: 0 auto;
            font-size: 1rem;
            font-weight: 400;
            line-height: 1.6;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.35);
        }

        .salary-kpis {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 0.9rem;
            margin-top: 1.25rem;
            max-width: 820px;
        }

        .salary-kpi {
            background: rgba(6, 18, 47, 0.62);
            border: 1px solid rgba(22, 211, 255, 0.28);
            border-radius: 12px;
            padding: 0.8rem 0.9rem;
            color: #fff;
            backdrop-filter: blur(4px);
        }

        .salary-kpi .label {
            display: block;
            text-transform: uppercase;
            font-size: 0.72rem;
            letter-spacing: 0.05em;
            opacity: 0.85;
        }

        .salary-kpi .value {
            font-size: 1.25rem;
            font-weight: 700;
        }

        .salary-block {
            background: #fff;
            border-radius: 14px;
            box-shadow: 0 8px 28px rgba(15, 23, 42, 0.08);
            padding: 1.6rem;
            height: 100%;
        }

        .salary-block h4 {
            font-size: 1.12rem;
            font-weight: 700;
            margin-bottom: 0.55rem;
            color: #0f172a;
        }

        .salary-block p {
            color: #64748b;
            margin-bottom: 0;
        }

        .salary-section {
            background: #f7f9fc;
            padding: 4.25rem 0;
        }

        .salary-section-title {
            font-size: 2rem;
            font-weight: 800;
            color: #0f172a;
            margin-bottom: 0.5rem;
        }

        .salary-section-subtitle {
            color: #64748b;
            margin-bottom: 2rem;
        }

        .salary-process-step {
            display: flex;
            gap: 0.9rem;
            align-items: flex-start;
            background: #fff;
            border: 1px solid #e5eaf1;
            border-radius: 12px;
            padding: 1rem 1.1rem;
        }

        .salary-process-step .num {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: linear-gradient(135deg, #10b7e9, #1d4ed8);
            color: #fff;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.9rem;
            flex-shrink: 0;
        }

        .salary-process-step h5 {
            margin: 0 0 0.2rem;
            font-size: 1rem;
            color: #0f172a;
            font-weight: 700;
        }

        .salary-process-step p {
            margin: 0;
            color: #64748b;
            font-size: 0.94rem;
        }

        .salary-cta-panel {
            border-radius: 16px;
            background: linear-gradient(135deg, #071943, #10307a);
            color: #fff;
            padding: 2rem;
            box-shadow: 0 14px 30px rgba(7, 25, 67, 0.33);
        }

        .profile-nav-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.35rem 0.8rem;
            border-radius: 999px;
            border: 1px solid rgba(255, 255, 255, 0.28);
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            text-decoration: none;
            transition: all 0.2s ease;
        }

        .profile-nav-btn:hover {
            background: rgba(255, 255, 255, 0.2);
            color: #fff;
        }

        .profile-nav-avatar {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.55);
        }

        .profile-nav-name {
            max-width: 120px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            font-size: 0.88rem;
            font-weight: 700;
        }

        @media (max-width: 991px) {
            .salary-kpis {
                grid-template-columns: 1fr;
            }

            .hero-tagline {
                font-size: 0.74rem;
                letter-spacing: 0.11em;
            }

            .salary-hero-title {
                font-size: clamp(1.7rem, 8vw, 3rem);
            }
        }
    </style>
</head>

<body>
<nav class="navbar navbar-expand-lg fixed-top glass-nav">
    <div class="container">
        <a class="navbar-brand fw-bold text-white" href="{{ path('salary_dashboard') }}">
            <i class="fas fa-wallet text-info me-2"></i> Decide$
        </a>

        <button class="navbar-toggler border-0 text-white" type="button" data-bs-toggle="collapse" data-bs-target="#salaryNav" aria-controls="salaryNav" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
        </button>

        <div class="collapse navbar-collapse" id="salaryNav">
            <ul class="navbar-nav mx-auto gap-lg-4">
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('salary_dashboard') }}">Home</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_about') }}">About</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_service') }}">Services</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_contact') }}">Contact</a></li>
            </ul>

            <div class="d-flex gap-2 align-items-center">
                <a href="{{ path('salary_profile') }}" class="profile-nav-btn" title="My profile">
                    <img src="{{ asset(current_user and current_user.image ? 'uploads/users/' ~ current_user.image : 'uploads/users/chat1.png') }}" alt="User profile" class="profile-nav-avatar">
                    <span class="profile-nav-name">{{ current_user and current_user.nom ? current_user.nom : 'My profile' }}</span>
                </a>
                <a href="{{ path('app_salary_expense_index') }}" class="btn btn-info rounded-pill px-4">Start Now</a>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger rounded-pill px-4">Logout</a>
            </div>
        </div>
    </div>
</nav>

<section class="hero-carousel">
    <div class="hero-overlay"></div>
    <div id="heroCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel" data-bs-interval="4800">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="hero-caption animate-text">
                    <span class="hero-tagline">SMART SALARY MANAGEMENT</span>
                    <h1 class="salary-hero-title">
                        Take control of your salary.<br>
                        <span class="accent">Track, plan and grow with confidence.</span>
                    </h1>
                    <p class="salary-hero-text">
                        Build a clear monthly strategy for income, spending and savings using one structured workspace.
                    </p>

                    <div class="salary-kpis">
                        <div class="salary-kpi">
                            <span class="label">Visibility</span>
                            <span class="value">100%</span>
                        </div>
                        <div class="salary-kpi">
                            <span class="label">Expense Control</span>
                            <span class="value">Real-time</span>
                        </div>
                        <div class="salary-kpi">
                            <span class="label">Planning</span>
                            <span class="value">Monthly</span>
                        </div>
                    </div>
                </div>

                <img src="{{ asset('img/a8.jpg') }}" class="d-block w-100 hero-img" alt="Salary management">
            </div>

            <div class="carousel-item">
                <div class="hero-caption animate-text">
                    <span class="hero-tagline">BETTER DECISIONS</span>
                    <h1 class="salary-hero-title">
                        Reduce financial stress.<br>
                        <span class="accent">Know exactly where your money goes.</span>
                    </h1>
                    <p class="salary-hero-text">
                        Identify unnecessary spending, optimize categories, and maintain a healthy monthly balance.
                    </p>
                </div>
                <img src="{{ asset('img/a9.jpg') }}" class="d-block w-100 hero-img" alt="Expense tracking">
            </div>

            <div class="carousel-item">
                <div class="hero-caption animate-text">
                    <span class="hero-tagline">LONG-TERM SECURITY</span>
                    <h1 class="salary-hero-title">
                        Turn salary into strategy.<br>
                        <span class="accent">Create stability and growth over time.</span>
                    </h1>
                    <p class="salary-hero-text">
                        Build emergency buffers, improve saving discipline, and prepare for future goals.
                    </p>
                </div>
                <img src="{{ asset('img/a10.jpg') }}" class="d-block w-100 hero-img" alt="Financial planning">
            </div>
        </div>
    </div>
</section>

<section class="salary-section">
    <div class="container">
        <div class="text-center mb-4">
            <h2 class="salary-section-title">Why This Workspace Works</h2>
            <p class="salary-section-subtitle">A cleaner, faster, and more reliable way to manage your salary month after month.</p>
        </div>

        <div class="row g-4 mb-4">
            <div class="col-lg-4">
                <div class="salary-block">
                    <h4><i class="fas fa-list-check text-info me-2"></i>Clear Structure</h4>
                    <p>Organize every income and expense entry with consistent categories and dates.</p>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="salary-block">
                    <h4><i class="fas fa-chart-line text-info me-2"></i>Actionable Insights</h4>
                    <p>Visualize spending patterns and identify where to optimize your monthly budget.</p>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="salary-block">
                    <h4><i class="fas fa-shield-heart text-info me-2"></i>Financial Confidence</h4>
                    <p>Make smarter decisions with a complete view of your personal financial health.</p>
                </div>
            </div>
        </div>

        <div class="row g-4 align-items-stretch">
            <div class="col-lg-7">
                <div class="salary-block h-100">
                    <h4 class="mb-3"><i class="fas fa-diagram-project text-info me-2"></i>How It Works</h4>
                    <div class="d-grid gap-3">
                        <div class="salary-process-step">
                            <span class="num">1</span>
                            <div>
                                <h5>Add income data</h5>
                                <p>Record salary and additional revenues in a structured format.</p>
                            </div>
                        </div>
                        <div class="salary-process-step">
                            <span class="num">2</span>
                            <div>
                                <h5>Track your expenses</h5>
                                <p>Capture each expense category to monitor spending behavior.</p>
                            </div>
                        </div>
                        <div class="salary-process-step">
                            <span class="num">3</span>
                            <div>
                                <h5>Review monthly balance</h5>
                                <p>Use net balance indicators to plan your next financial decisions.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-5">
                <div class="salary-cta-panel h-100 d-flex flex-column justify-content-between">
                    <div>
                        <h3 class="mb-2">Ready to manage your salary better?</h3>
                        <p class="mb-0" style="color: rgba(255,255,255,0.88);">
                            Open the Income & Expenses module and start organizing your financial flow today.
                        </p>
                    </div>
                    <div class="mt-4 d-flex flex-wrap gap-2">
                        <a href="{{ path('app_salary_expense_index') }}" class="btn btn-info rounded-pill px-4">
                            <i class="fas fa-play me-2"></i>Open Module
                        </a>
                        <a href="{{ path('app_service') }}" class="btn btn-outline-light rounded-pill px-4">
                            Learn More
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<footer class="bg-dark text-center py-4">
    <p class="text-white mb-0">© {{ "now"|date("Y") }} Decide$ — All rights reserved</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="{{ asset('js/theme-toggle.js') }}"></script>
</body>
</html>


