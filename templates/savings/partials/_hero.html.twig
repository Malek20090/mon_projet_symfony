<section class="hero">
  <div class="container text-center wrap">
    <h1>Savings & Goals</h1>
    <p>Track savings, create goals, contribute, and monitor progress — all in one place.</p>

    <div class="breadcrumbs">
      <a href="#">Home</a> / <a href="#">Modules</a> / <span class="active">Savings & Goals</span>
    </div>

    <div class="hero-actions">
  <a class="btn-primary2"
     href="{{ path('app_savings_index', { tab: 'savings' }) }}#deposit">
    ➕ Add Deposit
  </a>

  <a class="btn-soft"
     href="{{ path('app_savings_index', { tab: 'goals' }) }}#goal-form">
    🎯 Create Goal
  </a>

  <button class="btn-soft" type="button" id="openCalendar">📅 Calendar</button>
  <button class="btn-soft" type="button" id="openSimulation">📈 What-if?</button>
</div>

  </div>
</section>
